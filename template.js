const firstName = "Ashfiq";
const lastName = "Topu";
const fullName = firstName +" "+ lastName;
console.log(fullName);

const fullName2 = `${firstName} ${lastName} 
is a good boy`;
//no need of (\n)  Its multiLine
console.log(fullName2);